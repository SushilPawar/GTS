<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GTS">
        <activity android:name=".Add_cases" android:theme="@style/whitestatusbar"></activity>
        <activity
            android:name=".Support_activity"
            android:theme="@style/whitestatusbar"></activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/overall" />
        <activity
            android:name=".Splash_screen"
            android:theme="@style/loginpage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".User_login"
            android:theme="@style/loginpage" />
    </application>

</manifest>